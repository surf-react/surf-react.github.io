source "https://rubygems.org"

# Mirrors the exact Jekyll environment GitHub Pages uses to build the site.
gem "github-pages", group: :jekyll_plugins

# Needed for local preview on Ruby >= 3.0
gem "webrick", "~> 1.8"
